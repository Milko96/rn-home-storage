import React from 'react';
import { Text, View } from 'react-native';
import { useTheme } from 'styled-components/native';

const BuyListScreen = () => {
  const theme = useTheme();

  return <Text>Buy list works</Text>;
};

export default BuyListScreen;
