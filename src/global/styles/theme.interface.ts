export interface ITheme {
  background: string;
  primary: string;
  secondary: string;
  accent: string;
  shadow: string;
  placeholder: string;
  text: string;
  success: string;
}

export interface IFontTheme {
  fontFamily: string;
}